version: 1.0
provider:
  name: openfaas
  gateway: https://osm-faas.etica.ai
functions:
  osm-api-proxy:
    lang: python3-http
    handler: ./osm-api-proxy
    # image: EticaAI/osm-api-proxy:latest
    image: osm-api-proxy:latest
    annotations:
      # LABEL org.opencontainers.image.source https://github.com/OWNER/REPO
      # com.openfaas.ready.http.path: "/_/ready"
      # com.openfaas.ready.http.initialDelay: "5s"
      # com.openfaas.ready.http.periodSeconds: "2s"
      org.opencontainers.image.source: "https://github.com/fititnt/openstreetmap-faas-infra"
